{
  "name": "json-utilities",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "generate:icons": "bun scripts/generate-icons.mjs",
    "generate:datetime-icons": "bun scripts/generate-datetime-icons.mjs",
    "build:extension": "bun run generate:icons && vite build --config vite.extension.config.ts",
    "build:datetime-extension": "bun run generate:datetime-icons && vite build --config vite.datetime-extension.config.ts",
    "build:extensions": "bun run build:extension && bun run build:datetime-extension",
    "typecheck": "tsc -b --noEmit"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4"
  }
}
